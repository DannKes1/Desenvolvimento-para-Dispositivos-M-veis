# 📋 Formulário React Native

Este é um pequeno projeto de formulário mobile desenvolvido com **React Native** utilizando **Expo**.  
O foco está na validação de campos, aplicação de máscaras (CPF, telefone, CEP) e layout responsivo com visual limpo.

---

## 🧰 Tecnologias e Dependências

Para rodar o projeto, instale as seguintes dependências:

```bash
npm install formik yup
npm install react-native-masked-text
npx expo install react-native-paper react-native-vector-icons react-native-safe-area-context
